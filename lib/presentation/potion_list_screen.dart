import 'package:flutter/material.dart';

class PotionListScreen extends StatelessWidget {
  PotionListScreen({Key key}): super(key: key);

  @override
  Widget build(BuildContext context) {
    return new Text('TODO: PotionListScreen');
  }
}